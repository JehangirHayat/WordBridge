<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Default state (White border) -->
    <item android:state_enabled="null">
        <shape>
            <solid android:color="@android:color/transparent"/>
            <stroke android:color="@android:color/white" android:width="2dp"/>
            <corners android:radius="8dp"/>
        </shape>
    </item>

    <!-- State for when the answer is correct (Green border) -->
    <item android:state_enabled="true">
        <shape>
            <solid android:color="@android:color/transparent"/>
            <stroke android:color="@android:color/holo_green_light" android:width="2dp"/>
            <corners android:radius="8dp"/>
        </shape>
    </item>

    <!-- State for when the answer is wrong (Red border) -->
    <item android:state_enabled="false">
        <shape>
            <solid android:color="@android:color/transparent"/>
            <stroke android:color="@android:color/holo_red_light" android:width="2dp"/>
            <corners android:radius="8dp"/>
        </shape>
    </item>
</selector>
